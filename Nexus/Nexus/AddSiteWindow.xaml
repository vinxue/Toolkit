<Window x:Class="Nexus.AddSiteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Nexus"
        mc:Ignorable="d"
        Title="Add site" Height="240" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Name" FontSize="13" Margin="0,0,0,4" />
        <TextBox x:Name="NameBox" Grid.Row="1" Height="30" FontSize="13"
                 VerticalContentAlignment="Center" Padding="6,0" />

        <TextBlock Grid.Row="2" Text="URL" FontSize="13" Margin="0,14,0,4" />
        <TextBox x:Name="UrlBox" Grid.Row="3" Height="30" FontSize="13"
                 VerticalContentAlignment="Center" Padding="6,0"
                 Text="https://" />

        <TextBlock x:Name="ErrorText" Grid.Row="4"
                   Foreground="#DC2626" FontSize="12"
                   TextWrapping="Wrap"
                   Margin="0,10,0,0"
                   Visibility="Collapsed" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource SecondaryButton}"
                    Width="90" IsCancel="True" Click="Cancel_Click" />
            <Button Content="Add" Style="{StaticResource PrimaryButton}"
                    Width="90" Margin="10,0,0,0" IsDefault="True" Click="Add_Click" />
        </StackPanel>
    </Grid>
</Window>
