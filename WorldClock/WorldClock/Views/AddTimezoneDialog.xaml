<Window x:Class="WorldClock.Views.AddTimezoneDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Timezone"
        Width="480" Height="560"
        MinWidth="400" MinHeight="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="Transparent"
        Foreground="{DynamicResource PrimaryText}"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        FontSize="13"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        RenderOptions.ClearTypeHint="Enabled"
        Loaded="Window_Loaded">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Frosted background panel -->
        <Border Grid.RowSpan="4"
                Background="{DynamicResource WindowBackground}"
                CornerRadius="8"/>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{DynamicResource HeaderBackground}"
                BorderBrush="{DynamicResource SeparatorColor}"
                BorderThickness="0,0,0,1"
                CornerRadius="8,8,0,0"
                Padding="20,16">
            <StackPanel>
                <TextBlock Text="Add Timezone"
                           FontSize="18" FontWeight="SemiBold"
                           Foreground="{DynamicResource PrimaryText}"/>
                <TextBlock Text="Search and select a timezone to add to your clock."
                           FontSize="12" Foreground="{DynamicResource SecondaryText}"
                           Margin="0,3,0,0"/>
            </StackPanel>
        </Border>

        <!-- Search Box -->
        <Border Grid.Row="1" Margin="16,12,16,6"
                Background="{DynamicResource ControlBackground}"
                BorderBrush="{DynamicResource ControlBorder}"
                BorderThickness="1" CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <!-- Search icon -->
                <TextBlock Grid.Column="0" Text="&#xE721;" FontFamily="Segoe MDL2 Assets"
                           FontSize="14" VerticalAlignment="Center" Margin="10,0,8,0"
                           Foreground="{DynamicResource SecondaryText}"/>
                <TextBox Grid.Column="1"
                         x:Name="SearchBox"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         BorderThickness="0" Background="Transparent"
                         Foreground="{DynamicResource PrimaryText}"
                         CaretBrush="{DynamicResource PrimaryText}"
                         VerticalAlignment="Center"
                         Padding="0,8" FontSize="13"
                         Style="{DynamicResource PlainTextBoxStyle}"/>
                <!-- Clear button: only visible when search box has content -->
                <Button Grid.Column="2" x:Name="ClearSearchBtn"
                        Content="&#xE711;" FontFamily="Segoe MDL2 Assets"
                        FontSize="11" Width="28" Height="28"
                        Foreground="{DynamicResource SecondaryText}"
                        Cursor="Hand" Margin="0,0,4,0"
                        Click="ClearSearch_Click">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource IconButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SearchText}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>

        <!-- Timezone List -->
        <Border Grid.Row="2"
                Background="{DynamicResource ControlBackground}"
                BorderBrush="{DynamicResource ControlBorder}"
                BorderThickness="1" CornerRadius="6"
                Margin="16,0,16,12">
            <ListBox x:Name="TimezoneList"
                     ItemsSource="{Binding FilteredTimeZones}"
                     SelectedItem="{Binding SelectedTimezone, Mode=TwoWay}"
                     Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     VirtualizingStackPanel.IsVirtualizing="True"
                     VirtualizingStackPanel.VirtualizationMode="Recycling"
                     MouseDoubleClick="TimezoneList_MouseDoubleClick">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="12,8"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                            BorderThickness="0" CornerRadius="4" Margin="4,1"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ItemHoverBackground}"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentBackground}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding DisplayName}"
                                       FontSize="13" Foreground="{DynamicResource PrimaryText}"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding Id}"
                                       FontSize="11" Foreground="{DynamicResource SecondaryText}"
                                       Margin="0,1,0,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Buttons -->
        <Border Grid.Row="3"
                Background="{DynamicResource StatusBarBackground}"
                BorderBrush="{DynamicResource SeparatorColor}"
                BorderThickness="0,1,0,0"
                CornerRadius="0,0,8,8"
                Padding="16,10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" x:Name="SelectionHint"
                       Text="Double-click or press Add to confirm."
                       FontSize="11" Foreground="{DynamicResource SecondaryText}"
                       VerticalAlignment="Center"/>
            <Button Grid.Column="1" x:Name="CancelBtn"
                    Content="Cancel" Width="90" Height="34"
                    Style="{DynamicResource SecondaryButtonStyle}"
                    Click="Cancel_Click"/>
            <Button Grid.Column="3" x:Name="AddBtn"
                    Content="Add" Width="90" Height="34"
                    Style="{DynamicResource PrimaryButtonStyle}"
                    IsDefault="True"
                    Click="Add_Click"/>
        </Grid>
        </Border>
    </Grid>
</Window>
