<Window x:Class="FileForge.Views.PatchEntryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Patch Entry" Width="440" SizeToContent="Height" MaxHeight="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FontFamily="Segoe UI Variable,Segoe UI"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="OFFSET  (0x… or decimal)" Style="{StaticResource FieldLabelStyle}"/>
            <TextBox x:Name="txtOffset" Text="0x00000000" FontFamily="Consolas"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="NEW BYTES  (hex, space-separated  e.g. DE AD BE EF)"
                       Style="{StaticResource FieldLabelStyle}"/>
            <TextBox x:Name="txtBytes" Text="" FontFamily="Consolas"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Text="DESCRIPTION  (optional)" Style="{StaticResource FieldLabelStyle}"/>
            <TextBox x:Name="txtDesc" Text=""/>
        </StackPanel>

        <TextBlock x:Name="txtError" Grid.Row="3" Foreground="#C42B1C"
                   FontSize="12" TextWrapping="Wrap" VerticalAlignment="Center"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource SecondaryButtonStyle}"
                    Width="90" Margin="0,0,8,0" Click="BtnCancel_Click"/>
            <Button Content="OK" Style="{StaticResource PrimaryButtonStyle}"
                    Width="90" Click="BtnOK_Click"/>
        </StackPanel>
    </Grid>
</Window>
